Write your reflection here.

See reflection.md for what makes a good reflection — be specific, use the
terminology from this assignment (package, module, ETL, coercion, public API,
relative import, accumulator, group by, unit vs. integration test), and make it
actionable.

I learned a lot during this assignment, specifically with the use of packages. In this case it was the folder with the __init__.py file where we could load functions easily. This was with using the different relative imports such as the ones from .transform import clean_sales_data. The biggest struggle for me today was indentations. It took me a while to reconfigure the functions because I was getting many errors with my code being out of line. I think I need more help in seeing the connections in between each file and how they all work simultaneously. 